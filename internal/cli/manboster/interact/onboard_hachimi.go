package interact

import (
	"github.com/manboster/manboster/internal/config"
	"github.com/manboster/manboster/spec/cli"
)

// runOnboardHachimiConfig runs app Config
func runOnboardHachimiConfig(p cli.Provider) (config.HachimiConfig, error) {
	conf := config.HachimiConfig{}
	return conf, nil
}
