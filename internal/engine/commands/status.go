package commands

import (
	"context"

	"github.com/manboster/manboster/internal/chat"
)

// Status TODO: return current chats status
func Status(ctx context.Context, instance chat.Provider, msg *chat.Message) error {
	return nil
}
