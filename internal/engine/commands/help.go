package commands

import (
	"context"

	"github.com/manboster/manboster/internal/chat"
)

// Help is TODO: the helper command of manboster
func Help(ctx context.Context, instance chat.Provider, msg *chat.Message) error {
	return nil
}
