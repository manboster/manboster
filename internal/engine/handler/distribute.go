package handler

import (
	"context"

	"github.com/manboster/manboster/spec/chat"
)

func (h *Handler) DistributeFeedbackMsg(ctx context.Context, instance chat.Provider, msg *chat.Message) error {
	return nil
}
